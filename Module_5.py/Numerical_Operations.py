numbers = [10, 4, -1, 20, 5.5, 7, 1]
# if munber = [10, 4, -1, 20, 5.5, 7, 1, "Hi"] them TypeError
# Smallest number in the list
# min()
print(f"The smallest number in the list: {min(numbers)}")

# Biggest number in the list
# max()
print(f"The biggest number in the list: {max(numbers)}")

# Summation of all the elements in the list
# sum()
print(f"The sum of all the elements in the list: {sum(numbers)}")